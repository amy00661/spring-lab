spring:
  application:
    name: user-service
  profiles:
    active: dev
    
server:
  port: 8080
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

security:
  jwt:
    secret: change-me-super-secret-256-bit-key-which-is-long-enough
    expiration-ms: 86400000 #一天